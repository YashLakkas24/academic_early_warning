import RoutePlaceholder from "../../components/RoutePlaceholder/RoutePlaceholder";

/**
 * Placeholder only. The real Student Dashboard (Interest Quiz, Career
 * Analysis, Skill Gap) will be built in a future task.
 */
function StudentDashboard() {
  return (
    <RoutePlaceholder
      eyebrow="Student workspace"
      title="Student Dashboard"
      description="This is a placeholder route. The full dashboard — interest quiz, career analysis, and skill gap — will be built next."
    />
  );
}

export default StudentDashboard;
