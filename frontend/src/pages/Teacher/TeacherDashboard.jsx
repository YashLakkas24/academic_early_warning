import RoutePlaceholder from "../../components/RoutePlaceholder/RoutePlaceholder";

/**
 * Placeholder only. The real Teacher Dashboard (Risk Students, Upload
 * Report, Student Profile, etc.) will be built in a future task.
 */
function TeacherDashboard() {
  return (
    <RoutePlaceholder
      eyebrow="Teacher workspace"
      title="Teacher Dashboard"
      description="This is a placeholder route. The full dashboard — risk students, report uploads, and student profiles — will be built next."
    />
  );
}

export default TeacherDashboard;
